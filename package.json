{
  "name": "neon-sketch-arena-root",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd backend && npm install --omit=dev",
    "build": "cd frontend && npm run build",
    "start": "node backend/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
